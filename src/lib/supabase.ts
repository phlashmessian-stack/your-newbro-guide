// Supabase removed. This file is kept only to avoid accidental leakage of keys.
// The deployed version uses `public/js/supabase-config.js` (PHP+SQLite) instead.
export const supabase = null as any;
